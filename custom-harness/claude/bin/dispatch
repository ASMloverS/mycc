#!/usr/bin/env bash
# Wrapper: prefer dispatch.exe, fall back to dispatch.py
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
EXE="$DIR/dispatch.exe"
if [[ -f "$EXE" ]]; then
    exec "$EXE" "$@"
else
    exec python "$DIR/dispatch.py" "$@"
fi
